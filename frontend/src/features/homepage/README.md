# Homepage — sekce, zprávy, RSS

**Routes:** `/s/:slug`, `/messages`, `/my-rss`

| Stránka | API |
|---------|-----|
| `SectionPage` | `/api/sections`, `/api/homepage/render` |
| `MessagesPage` | `/api/chat/*` ❌ stub |
| `MyRssFeedsPage` | `/api/rss/*` ✅ |
