# Auth — přihlášení, nastavení, reset hesla

**Routes:** `/settings`, `/verify-email`, `/reset-password`

| Komponenta | API |
|------------|-----|
| `SettingsPage` | `/api/auth/me`, profile |
| `contexts/AuthContext` (v `src/contexts/`) | JWT cookies + CSRF |

Dev login: `admin@bankintel.local` / `admin123`
