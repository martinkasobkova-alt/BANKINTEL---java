# Local dev without Docker — DataSource is provided by LocalEmbeddedPostgresConfig.
spring:
  config:
    activate:
      on-profile: local
  flyway:
    enabled: true
    locations: classpath:db/migration
  jpa:
    hibernate:
      ddl-auto: validate

logging:
  level:
    org.flywaydb: INFO
